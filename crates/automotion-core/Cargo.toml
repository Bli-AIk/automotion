[package]
name = "automotion-core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]

[[bin]]
name = "uniffi-bindgen"
path = "src/bin/uniffi-bindgen.rs"

[dependencies]
regex = "1"
chrono = "0.4"
zip = "2"
roxmltree = "0.20"
uniffi = { version = "0.29", features = ["cli"] }
